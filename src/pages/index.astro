---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Navigationbar from '../components/Navigationbar.astro';
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <Navigationbar/>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>大学生作业互助平台</title>
    <style>
        body, h1 {
            margin: 0; /* 移除默认边距 */
            padding: 0;
        }

        body {
            font-family: 'Open Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif; /* 设置默认字体族 */
            background-color: #f4f8f9; /* 背景颜色 */
            text-align: center; /* 使页面内容居中 */
        }

        .header-title {
            color: #0a0a0b; /* 文字颜色，深蓝色 */
            font-size: 60px; /* 字体大小 */
            letter-spacing: 10px; /* 字间距 */
            text-transform: uppercase; /* 文本转换为大写，更具视觉效果 */
            margin-top: 50px; /* 上边距 */
            font-weight: 00; /* 字体粗细 */
        }

        /* 如果需要添加渐变效果 */
        .header-title {
            background: -webkit-linear-gradient(90deg, #4a7d57, #7cb14a);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            color: transparent;
        }

        /* 响应式设计 */
        @media (max-width: 768px) {
            .header-title {
                font-size: 48px; /* 移动设备上减小字体大小 */
                margin-top: 30px; /* 移动设备上减小上边距 */
            }
        }
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f8f9;
            color: #333;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        .container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 20px;
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .section {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .section:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .section h2 {
            font-size: 24px;
            margin-bottom: 10px;
            color: #4a4a4a;
        }

        .section p {
            font-size: 16px;
            color: #666;
        }

        .section .icon {
            font-size: 48px;
            color: #ff6347; /* 番茄色 */
            margin-bottom: 10px;
        }

        .footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            position: absolute;
            bottom: 0;
            width: 100%;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .bounce {
            animation: bounce 2s infinite;
        }
        .footer {
            background-color: #98b497;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            position: fixed; /* 固定在底部 */
            left: 0;
            bottom: 0;
            width: 100%;
        }

        /* 如果您想要在滚动时始终保持页脚在视窗底部，
           但同时希望它在页面内容下方有足够的空间，
           您可以给<body>或.container添加padding-bottom。 */

        body {
            padding-bottom: 100px; /* 根据页脚高度调整 */
        }

        /* 或者，如果您只希望在页面内容下方有空间，
           而不想让整个<body>都有padding，您可以给最后一个
           内容容器添加padding-bottom */

        /* .last-container {
            padding-bottom: 100px;
        } */
    </style>
</head>
<body>
    <div class="header">
        <h1 class="header-title">大学生互助平台</h1>
    </div>

    <div class="container">
        <!-- 论坛讨论 -->
        <div class="section" onclick="location.href='/push';">
            <div class="icon bounce">💡</div>
            <h2>解答</h2>
            <p>在这里你可以上传你的作品，对其进行描述和制定授权价格</p>
        </div>

        <!-- 提问 -->
        <div class="section" onclick="location.href='/ask';">
            <div class="icon bounce">❓</div>
            <h2>提问</h2>
            <p>遇到难题？来这里寻找相应的答案，一定有你想要的优秀作品。</p>
        </div>

        <!-- 解答 -->
        <div class="section" onclick="location.href='/forum';">
            <div class="icon bounce">🎓</div>
            <h2>论坛讨论</h2>
            <p>分享你的知识，帮助他人解决问题。</p>
        </div>

        <!-- 个人空间 -->
        <div class="section" onclick="location.href='/profile';">
            <div class="icon bounce">👤</div>
            <h2>个人空间</h2>
            <p>管理你的个人信息和交易。</p>
        </div>
    </div>

    <div class="footer">
        <p>版权所有 © 2023 大学生作业互助平台</p>
    </div>
</body>
</html>